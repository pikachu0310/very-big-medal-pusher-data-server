name: Main

on:
  push:
    branches:
      - main

jobs:
  build:
    name: Main Build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-go@v7
        with:
          go-version-file: ./go.mod
      - run: make build
